/**
 * 编写一个程序，通过已填充的空格来解决数独问题
 *
 * 数独遵循法则：
 *  1. 数字1-9在每一列每一行只能出现一次
 *  2. 数字1-9在每一个以粗实线分割的3x3宫内只能出现一次
 *  空格用 '.' 表示
 */

/**
 * 假设：
 *   给定的数独序列只包含1-9和'.'
 *   给定的数独有唯一解
 *   给定的形式永远是9x9
 */
/**
 *
 * @param {number[][]} board
 * @returns {void} Do not return anything, modify board in-place instead.
 */
const solveSudoku = (board) => {};
